<form method="post" action="{{ path('avaliation_delete', {'id': avaliation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avaliation.id) }}">
    <button class="btn">Delete</button>
</form>
