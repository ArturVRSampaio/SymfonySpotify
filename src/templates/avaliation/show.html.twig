{% extends 'base.html.twig' %}

{% block title %}Avaliation{% endblock %}

{% block body %}
    <h1>Avaliation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avaliation.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('avaliation_index') }}">back to list</a>

    {{ include('avaliation/_delete_form.html.twig') }}
{% endblock %}
